<p id="notice"><%= notice %></p>

<%= link_to 'Cadastrar', new_sub_equipe_path, class: 'btn btn-success' %>


<table class="table table-hover table-striped table-bordered">
  <thead class="bordered-blueberry">
  <tr>
    <th>#</th>
    <th>Descrição </th>
    <th>Equipe</th>
    <th width="20"></th>
    <th width="20"></th>
    <th width="20"></th>

  </tr>
  </thead>

  <tbody>
  <% @sub_equipes.each do |sub_equipe| %>
      <tr>
        <td><%= sub_equipe.id %></td>
        <td><%= sub_equipe.descricao %></td>
        <td><%= sub_equipe.equipe %></td>
        <td>
          <%= link_to sub_equipe, title: 'Visualizar', class: 'btn btn-default btn-xs shiny icon-only success' do %>
              <%= fa_icon 'search' %>
          <% end %>
        </td>
        <td>
          <%= link_to edit_sub_equipe_path(sub_equipe), title: 'Editar', class: 'btn btn-default btn-xs shiny icon-only success' do %>
              <%= fa_icon 'pencil' %>
          <% end %>
        </td>
        <td>
          <%= link_to sub_equipe, title: 'Editar', class: 'btn btn-default btn-xs shiny icon-only success', method: :delete, data: { confirm: 'Are you sure?' } do %>
              <%= fa_icon 'close' %>
          <% end %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>
